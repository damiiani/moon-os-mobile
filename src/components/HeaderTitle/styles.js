import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
  },
  text: {
    marginStart: 10,
    fontWeight: 'bold',
    fontSize: '20px',
    color: '#ffffff',
  },
});

export default styles;
